  <h2 class="text-2xl mb-4 font-bold"><%= pluralize(quiz.players.count, "Player") %> Joined:</h2>
  <div class="grid grid-cols-8 gap-4 mb-6">
    <% quiz.players.each do |player| %>
      <p class="p-2 rounded-md border-1 border-slate-50 bg-slate-100/50 text-slate-950 font-bold text-lg text-center"><%= player.name %></p>
    <% end %>
  </div>